<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Clawix.App.Views.QuickAskWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Quick Ask">
  <Grid Padding="16" RowSpacing="8">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <TextBox Grid.Row="0" x:Name="QueryBox" PlaceholderText="Ask anything..." KeyDown="QueryBox_KeyDown" />
    <ScrollViewer Grid.Row="1">
      <TextBlock x:Name="ResultText" TextWrapping="Wrap" />
    </ScrollViewer>
    <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8">
      <Button x:Name="CameraBtn" Content="📷" Click="Camera_Click" />
      <Button x:Name="AttachBtn" Content="📎" Click="Attach_Click" />
      <Button x:Name="SendBtn" Content="Send" Style="{ThemeResource AccentButtonStyle}" Click="Send_Click" />
    </StackPanel>
  </Grid>
</Window>
