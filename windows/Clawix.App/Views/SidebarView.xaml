<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Clawix.App.Views.SidebarView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:Clawix.Core.Models"
    xmlns:views="using:Clawix.App.Views">
  <Grid Padding="8" Background="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <Grid Grid.Row="0" Margin="6,12,6,8">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <TextBlock Grid.Column="0" Text="Clawix" FontWeight="SemiBold" FontSize="20" />
      <Button Grid.Column="1" Click="Settings_Click" ToolTipService.ToolTip="Settings">
        <FontIcon Glyph="&#xE713;" />
      </Button>
    </Grid>
    <TextBox Grid.Row="1" x:Name="SearchBox" PlaceholderText="Search chats" Margin="6,4,6,8" TextChanged="SearchBox_TextChanged" />
    <TextBlock Grid.Row="2" x:Name="PinnedHeader" Text="Pinned" Margin="8,4,8,4" Style="{ThemeResource ClawixCaption}" />
    <ListView Grid.Row="3" x:Name="PinnedList"
              MaxHeight="128"
              ItemsSource="{x:Bind PinnedSessions, Mode=OneWay}"
              SelectionChanged="PinnedList_SelectionChanged"
              SelectionMode="Single">
      <ListView.ItemTemplate>
        <DataTemplate x:DataType="models:WireSession">
          <StackPanel Padding="6,4">
            <TextBlock Text="{x:Bind Title}" FontWeight="SemiBold" />
            <TextBlock Text="{x:Bind LastMessagePreview}" Opacity="0.6" TextTrimming="CharacterEllipsis" />
          </StackPanel>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
    <TextBlock Grid.Row="4" x:Name="ProjectsHeader" Text="Projects" Margin="8,4,8,4" Style="{ThemeResource ClawixCaption}" />
    <ListView Grid.Row="5" x:Name="ProjectList"
              MaxHeight="154"
              ItemsSource="{x:Bind Projects, Mode=OneWay}"
              SelectionChanged="ProjectList_SelectionChanged"
              SelectionMode="Single">
      <ListView.ItemTemplate>
        <DataTemplate x:DataType="models:WireProject">
          <StackPanel Padding="6,4">
            <TextBlock Text="{x:Bind Title}" FontWeight="SemiBold" />
            <TextBlock Text="{x:Bind Cwd}" Opacity="0.6" TextTrimming="CharacterEllipsis" />
          </StackPanel>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
    <Button Grid.Row="6" x:Name="AllChatsButton" Content="All chats" HorizontalAlignment="Stretch" Margin="6,4,6,6" Click="AllChats_Click" />
    <ListView Grid.Row="7" x:Name="ChatList"
              ItemsSource="{x:Bind Sessions, Mode=OneWay}"
              SelectionChanged="ChatList_SelectionChanged"
              SelectionMode="Single">
      <ListView.ItemTemplate>
        <DataTemplate x:DataType="models:WireSession">
          <StackPanel Padding="6,4">
            <TextBlock Text="{x:Bind Title}" FontWeight="SemiBold" />
            <TextBlock Text="{x:Bind LastMessagePreview}" Opacity="0.6" TextTrimming="CharacterEllipsis" />
          </StackPanel>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
    <Button Grid.Row="8" Content="New chat" HorizontalAlignment="Stretch" Margin="6" Click="NewChat_Click" />
    <views:RateLimitsWidget Grid.Row="9" x:Name="RateLimits" Margin="6,8,6,4" />
    <TextBlock Grid.Row="10" x:Name="BridgeStatusText" Margin="6,4,6,6" Style="{ThemeResource ClawixCaption}" />
  </Grid>
</UserControl>
