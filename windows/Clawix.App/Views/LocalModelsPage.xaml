<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Clawix.App.Views.LocalModelsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:core="using:Clawix.Core"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <ScrollViewer Padding="24">
    <StackPanel Spacing="16" MaxWidth="720">
      <TextBlock Text="Local models" FontSize="22" FontWeight="SemiBold" />
      <TextBlock Opacity="0.7" Text="Run inference on your machine. Recommended for users with a discrete GPU." />
      <TextBlock x:Name="SummaryText" Style="{ThemeResource ClawixCaption}" />
      <ListView x:Name="ModelList" ItemsSource="{x:Bind Models, Mode=OneWay}">
        <ListView.ItemTemplate>
          <DataTemplate x:DataType="core:LocalModelInfo">
            <Grid Padding="8,6" ColumnSpacing="12">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <StackPanel Grid.Column="0" Spacing="2">
                <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" />
                <TextBlock Text="{x:Bind FileName}" Style="{ThemeResource ClawixCaption}" />
              </StackPanel>
              <TextBlock Grid.Column="1" Text="{x:Bind DisplaySize}" VerticalAlignment="Center" Style="{ThemeResource ClawixCaption}" />
              <TextBlock Grid.Column="2" Text="{x:Bind Status}" VerticalAlignment="Center" />
            </Grid>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
      <StackPanel Orientation="Horizontal" Spacing="8">
        <Button Content="Refresh" Click="Refresh_Click" />
        <Button Content="Open models folder..." Style="{ThemeResource AccentButtonStyle}" Click="OpenModelsFolder_Click" />
      </StackPanel>
    </StackPanel>
  </ScrollViewer>
</Page>
