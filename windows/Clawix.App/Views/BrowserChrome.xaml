<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Clawix.App.Views.BrowserChrome"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Padding="8" ColumnSpacing="8">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto" />
      <ColumnDefinition Width="Auto" />
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="Auto" />
    </Grid.ColumnDefinitions>
    <Button Grid.Column="0" x:Name="BackButton" Content="←" Click="Back_Click" />
    <Button Grid.Column="1" x:Name="ForwardButton" Content="→" Click="Forward_Click" />
    <TextBox Grid.Column="2" x:Name="UrlBox" PlaceholderText="URL" KeyDown="UrlBox_KeyDown" />
    <Button Grid.Column="3" Content="↻" Click="Reload_Click" />
  </Grid>
</UserControl>
