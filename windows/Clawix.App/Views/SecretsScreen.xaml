<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Clawix.App.Views.SecretsScreen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Padding="24" RowSpacing="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <TextBlock Grid.Row="0" Text="Secrets vault" FontSize="22" FontWeight="SemiBold" />
    <TextBlock Grid.Row="1" Opacity="0.7" Text="Secrets are not enabled on Windows yet. When enabled, this screen must project ClawJS Secrets rather than write a Windows-local vault." TextWrapping="Wrap" />
    <ListView Grid.Row="2" x:Name="SecretsList" />
    <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="8">
      <Button Content="Add secret" Click="Add_Click" Style="{ThemeResource AccentButtonStyle}" IsEnabled="False" />
      <Button Content="Recovery phrase" Click="Recovery_Click" IsEnabled="False" />
      <Button Content="Lock vault" Click="Lock_Click" IsEnabled="False" />
    </StackPanel>
  </Grid>
</UserControl>
